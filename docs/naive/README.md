# Наивный прогон

**Дата:** 2026-08-16  
**Промпт (один, без SPEC и без Части II):**  
«Сделай простой трекер привычек в браузере»

**Результат:** `docs/naive/index.html` — один файл, список привычек, галочка, `localStorage`.

**Чего нет (для сравнения с прогоном по спеке):**
- фокус на одной привычке и переключение;
- streak;
- уникальность имён;
- защита от будущей даты;
- безопасный разбор битого JSON;
- состояние «пусто» с отдельным CTA / демо из 5;
- React / адаптив как контракт 360/1440.

Открыть: просто файл в браузере или любой статический сервер.
