# Журнал инструментов

> Только дополняется, старые записи не редактируются. Сюда попадает всё, что вы
> добавили в проект или окружение ради этой задачи: библиотеки, CLI-утилиты,
> линтеры, MCP-серверы, плагины и скиллы агента, хуки, внешние сервисы, контейнеры.
>
> Разовый вызов уже установленного инструмента сюда не пишется — он идёт
> в таблицу инструментов внутри файла сессии.
>
> Тупиковая попытка — тоже запись: поставили, не подошло, сняли. Это как раз интересно.

## Шаблон записи

```markdown
## YYYY-MM-DD · Сессия N · <инструмент> vX.Y

- **Тип:** библиотека / CLI / MCP / плагин / скилл / хук / сервис
- **Установка:** `<точная команда>`
- **Зачем:** какую задачу решает
- **Область:** проект или глобально
- **Проверка:** чем убедились, что работает
```

Удалённый или заменённый инструмент фиксируется так же — с пометкой
**удалён** / **заменён на X** и причиной.

---

## 2026-08-16 · Сессия 1 · pymupdf (user install)

- **Тип:** библиотека / CLI
- **Установка:** `pip3 install --user pymupdf`
- **Зачем:** извлечь текст из `homework.pdf` (в комплекте нет `.md` версии ДЗ)
- **Область:** глобально (user site-packages), не в репозитории проекта
- **Проверка:** `page.get_text()` вернул читаемый текст всех страниц ДЗ

## 2026-08-16 · Сессия 1 · Vite + React (create-vite)

- **Тип:** библиотека / CLI
- **Установка:** `npm create vite@latest app -- --template react-ts` затем `cd app && npm install`
- **Зачем:** стек приложения трекера привычек по Части II `AGENTS.md`
- **Область:** проект (`app/`)
- **Проверка:** `npm run build` успешен; `npm run dev` → http://localhost:5173/ HTTP 200; react@19.2.8, vite@8.2.1, typescript@6.0.3

## 2026-08-16 · Сессия 2 · Syne + Outfit (self-hosted woff2)

- **Тип:** ресурс / шрифты
- **Установка:** скачаны с jsDelivr fontsource в `app/public/fonts/` (без npm-пакета и без runtime CDN)
- **Зачем:** выразительная типографика дизайн-прохода при запрете CDN в AGENTS.md
- **Область:** проект
- **Проверка:** `@font-face` в CSS; скриншоты `docs/screenshots/after/` показывают крупный brand Syne
